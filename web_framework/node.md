# Node.js(Gulp)導入

> node.jsとnpmのインストール
  https://nodejs.org/ja/

> node.jsとnpmの確認
  node --version

> gulpのインストール
  npm init -y
  npm install gulp -D
  npx gulp --version
  > 必要なパッケージのインストール
    npm install gulp-file-include -D